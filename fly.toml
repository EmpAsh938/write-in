# fly.toml file generated for writein-app on 2022-11-04T14:46:34+05:45

app = "writein-app"
kill_signal = "SIGINT"
kill_timeout = 5
processes = []

[env]
    NODE_ENV = "production"
    JWT_SECRET = "apple1234"
    MONGO_URI = "mongodb+srv://empash-admin:empash-admin@cluster0.7ywclsq.mongodb.net/?retryWrites=true&w=majority"

[experimental]
  allowed_public_ports = []
  auto_rollback = true

[[services]]
  http_checks = []
  internal_port = 8080
  processes = ["app"]
  protocol = "tcp"
  script_checks = []
  [services.concurrency]
    hard_limit = 25
    soft_limit = 20
    type = "connections"

  [[services.ports]]
    force_https = true
    handlers = ["http"]
    port = 80

  [[services.ports]]
    handlers = ["tls", "http"]
    port = 443

  [[services.tcp_checks]]
    grace_period = "1s"
    interval = "15s"
    restart_limit = 0
    timeout = "2s"
